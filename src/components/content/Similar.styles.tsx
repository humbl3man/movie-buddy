import styled from 'styled-components';

export const StyledSimilarWidgetSlider = styled.div`
  padding-left: 2rem;
  padding-right: 2rem;
  p {
    color: var(--primary100);
  }
`;

export const StyledSimilarWidgetImage = styled.div`
  img {
    display: block;
    max-width: 150px;
    height: auto;
  }
`;
